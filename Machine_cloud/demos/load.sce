// Demo script

toolbox_basedir 		= input('Base directory for toolbox : ', "string")
public_key 				= input('Public key stored on the server : ', "string")
getd(toolbox_basedir + '/macros')

loader(public+key, toolbox_basedir);
